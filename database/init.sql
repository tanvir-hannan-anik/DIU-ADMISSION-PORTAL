-- ─────────────────────────────────────────────────────────────────────────────
-- DIU University Automation System — MySQL Init Script
-- Hibernate (ddl-auto=update) creates tables automatically.
-- This file only creates the database — indexes/views are added after startup.
-- ─────────────────────────────────────────────────────────────────────────────

CREATE DATABASE IF NOT EXISTS university_automation
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE university_automation;
